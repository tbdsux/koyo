{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".output/**", ".svelte-kit/**"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "cache": false
    }
  }
}
